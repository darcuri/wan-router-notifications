heartbeat:
  expected_interval: 60
  missed_threshold: 3
  listen_host: "100.64.0.1"
  listen_port: 8080

external_probe:
  enabled: true
  target_ip: "203.0.113.1"
  interval: 300
  timeout: 10

dns_monitor:
  enabled: true
  hostname: "example.duckdns.org"
  expected_ip: "203.0.113.1"
  interval: 300
